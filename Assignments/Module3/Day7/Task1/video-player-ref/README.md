# 🎥 Custom Video Player using useRef (React + Vite)

## 📌 Objective
To understand how the `useRef` hook can directly control a DOM element and modify UI behavior **without using React state**.

This project demonstrates a custom video player built using React, where play, pause, forward, and rewind functionalities are handled using `useRef`.

---

## 🛠️ Tech Stack
- React
- Vite
- JavaScript
- CSS

---

## 📂 Project Setup

### 1️⃣ Clone the Repository
```bash
git clone <your-github-repo-link>
cd video-player-useref
